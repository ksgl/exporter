{
    "conn": "user=ksu password='pswd' dbname='parkdb' sslmode=disable",
    "output_dir": "./result",
    "tables": [
      {
        "name": "posts",
        "query": "select * from posts",
        "max_lines": 10000
      }
    ]
}